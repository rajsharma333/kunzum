pipeline {
     agent any
     stages {
        stage("Build") {
            steps {
                echo "build done"
            }
        }
        stage("Deploy") {
            steps {
                echo "deployed"
            }
        }
    }
}
